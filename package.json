{
  "name": "red-rings-axon",
  "version": "1.0.0",
  "description": "Axon transport for red-rings (backend)",
  "main": "index.js",
  "scripts": {
    "prepublishOnly": "npm install --only=dev && rm -f package-lock.json && coffee -c *.coffee.md",
    "pretest": "npm install && npm run prepublishOnly && coffee -c test/*.coffee.md",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "github.com/shimaore/red-rings-axon"
  },
  "keywords": [
    "Axon",
    "red-rings",
    "CCNQ4"
  ],
  "author": "Stéphane Alnet <stephane@shimaore.net>",
  "license": "Unlicense",
  "dependencies": {
    "abrasive-ducks-transducers": "^2.1.2",
    "axon": "^2.0.3",
    "escape-regexp": "0.0.1",
    "red-rings": "^1.1.4"
  },
  "devDependencies": {
    "coffeescript": "^2.2.4",
    "mocha": "^5.1.1"
  }
}
